package pk12;

public class CalculatorExam {
	/* Calculator 클래스를 만들고 getResult() 함수를 정의
	 * 반환형은 정수인자(n1, n2)를 받고 -1로 리턴
	 * 
	 * CalPlus 클래스를 만들어 CalculatorExam 클래스를 상속받아라
	 * 오버라이딩을 이용하여 CalculatorExam의 getResult() 함수를 인자로 받은 n1과 n2를 더해주는 함수로 만들어라
	 * 물론 리턴값은 -1이면 안된다
	 * 
	 * CalMinus 클래스를 만들어 CalculatorExam 클래스를 상속받아라
	 * 오버라이딩을 이용하여 CalculatorExam의 getResult() 함수를 인자로 받은 n1과 n2를 빼주는 함수로 만들어라*/
	public int getResult(int n1, int n2) {
		return -1;
	}
}
